<snippet>
	<content><![CDATA[
<f:if condition="{${1:condition}}">
    <f:then>
        ${2:then}
    </f:then>   
    <f:else>
        ${3:else}
    </f:else>
</f:if>
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>f:if</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<!--scope>source.php</scope-->
</snippet>